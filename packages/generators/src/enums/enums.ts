export enum TipoDeArquivo{
    handler = 'handler',
    event = 'event',
    command = 'command',
    service = 'service'
}

export enum TipoDeBase{
    manual = 'manual',
    messages = 'messages',
}

export enum TipoDeExportacao{
    manual = 'manual',
    mirror = 'mirror',
}